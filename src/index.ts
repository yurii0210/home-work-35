import './styles.scss';
import './styles.less';

const app = document.getElementById('app');
if (app) {
  app.textContent = 'Webpack + TypeScript + Babel + ESLint + SCSS + LESS works!';
}

console.log('Hello from TypeScript!');
